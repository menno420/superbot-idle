- `claude/theme-parity-guard` · **test: durable loader↔schema PARITY-GUARD suite (capstone). A schema-introspection meta-check classifies every schema-declared constraint keyword as either loader-re-checked or a reviewed gate-only/structural-sugar allow-list entry, so a NEW schema constraint added WITHOUT matching loader enforcement reds a test and forces a human decision — pinning the class of bugs (embed_color format, duplicate keys, base_rate/rate_multiplier bounds) closed across #164/#166/#167/#168 so it can never silently regress. PLUS closes four untested loader reference-integrity guards (upgrades[].target, prestige.currency, prestige.measures, prestige currency==measures differ). TEST-ONLY + doc count bump; no engine/loader/gate behavior change** · tests/test_theme_parity_guard.py · docs/current-state.md · 2026-07-18
