- `claude/theme-base-rate-max` · **engine: enforce the schema-declared `base_rate` UPPER bound (maximum 1000) at load time in idle_engine.theme.load_theme — the loader re-checks positivity (lower bound) but not `<= 1000`, so a pack loaded outside the jsonschema gate silently smuggles an out-of-bounds economy rate; mirror the balance-bounds defense-in-depth re-check (loader-local constants, no tools/ import)** · idle_engine/theme.py · tests/test_theme_loader_guards.py · docs/current-state.md · 2026-07-18
