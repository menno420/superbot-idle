# Claim: install auto-merge-enabler workflow

- `claude/install-auto-merge-enabler` · **CI-config** — install `.github/workflows/auto-merge-enabler.yml` (fm ORDER 029, owner directive 2026-07-12; adapted from idea-engine) · `.github/workflows/` + session plumbing only · 2026-07-12
