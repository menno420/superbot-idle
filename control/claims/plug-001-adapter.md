# Claim — `plug-001-adapter`

- `plug-001-adapter` · **plugin adapter slice** — thin `plugin/` shell exporting a `SubsystemManifest` (command + status panel + `idle` capability) against the binding contract · `plugin/` subtree + `plugin/tests/` · 2026-07-12
