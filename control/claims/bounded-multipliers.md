- `bounded-multipliers` · **bounded theme-multiplier mechanism** — schema v1 additive optional `balance` block: per-generator `rate_multiplier_pct` with SCHEMA-DECLARED hard bounds 90..110 (100 = neutral, absent = neutral); loader validates bounds independently of the gate; engine folds the factor into the single-floor rate composition (`// 10^8`); catalog stays neutral — non-neutral values are sim-gated (Q-0264) · idle_engine/{theme,engine,state}.py, schema/theme.schema.json, docs/theme-schema.md, docs/design/, tools/theme_gate.py, tests/ · 2026-07-11
