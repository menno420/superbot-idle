# Claim: setup-code-test-vectors

- `setup-code-test-vectors` · **cross-language setup-code test vectors** — machine-readable `tests/vectors/setup-codes.v1.json` (valid + tolerance + error vectors, all 6 packs) + deterministic generator + regenerate-or-red tests + doc pointer · tests/vectors/, tools/gen_setup_vectors.py, tests/test_setup_vectors.py, docs/provisioning.md · 2026-07-11
