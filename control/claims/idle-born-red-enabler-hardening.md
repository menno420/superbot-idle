# Claim — born-red enabler-guard hardening

- `claude/idle-born-red-enabler-hardening` · **enabler card-guard hardening** — drafted-status refusal + badge-variant-tolerant Status parsing in the auto-merge enabler's card skip (port of superbot-games @ d6a9526 semantics) · .github/workflows/auto-merge-enabler.yml + .sessions/ + control/claims/ · 2026-07-13
