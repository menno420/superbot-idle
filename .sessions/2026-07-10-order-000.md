# 2026-07-10 — ORDER 000: walking skeleton

> **Status:** `in-progress`

- **📊 Model:** fable-5 · high · idle-engine seat (ORDER 000 builder) · 2026-07-10 ~23:46Z (`date -u`)

## Plan

Deliver the ORDER 000 walking skeleton as one PR on this branch:

1. `themes/egg-farm.yaml` — first data-only theme pack (theme id/name, one
   generator "chicken coop", one currency "eggs", flavor/emoji/embed color).
2. `idle_engine/` — deterministic pure-domain core: game state, `tick(state, dt)`
   applying generator production to currency balances, offline-progress
   calculation (closed-form, integer-safe), and a theme loader supplying ALL
   player-visible nouns. Zero theme nouns in engine source (CORE/SKIN split).
3. `tests/` — pytest: tick accrual over n ticks, offline == loop result,
   determinism, theme-noun mapping, and a guard test grepping engine source
   for egg-farm nouns.
4. theme-gate v0: `tools/theme_gate.py` validator + NEW workflow
   `.github/workflows/theme-gate.yml` (substrate-gate.yml is kit-owned,
   untouched).
5. Verify: `python3 -m pytest -q && python3 bootstrap.py check --strict` green,
   then flip this card `complete` as the last commit.

## 💡 Session idea

[[fill at close-out]]

## ⟲ Previous-session review

[[fill at close-out]]
