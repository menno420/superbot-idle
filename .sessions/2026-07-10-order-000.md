# 2026-07-10 — ORDER 000: walking skeleton

> **Status:** `complete`

- **📊 Model:** fable-5 · high · idle-engine seat (ORDER 000 builder) · 2026-07-10 23:46–23:5xZ (`date -u`)

## What happened

Delivered the ORDER 000 walking skeleton in one PR (this branch), after a
control fast-lane PR (#1, merged) claimed the order:

1. `themes/egg-farm.yaml` — first data-only theme pack: theme "Egg Farm"
   (id `egg-farm`, 🥚, embed color `#F5C542`), currency `primary` → "eggs",
   generator `tier1` → "chicken coop" (produces `primary`, base_rate 1).
2. `idle_engine/` — deterministic pure-domain core: `GameState` /
   `GeneratorSpec` (state.py), `tick(state, specs, dt)`, closed-form
   `offline_progress(state, specs, last_seen, now)` and
   `apply_offline_progress` (engine.py) — integer-only math, no wall clock,
   no I/O, no chat-platform calls; `theme.py` loads a YAML pack and supplies
   ALL player-visible nouns onto opaque engine ids.
3. `tests/` — 24 pytest cases: tick accrual over n ticks; offline
   closed-form == 3607 looped 1-second ticks; determinism (repeat runs
   byte-identical); theme noun mapping (`primary`→"eggs", `tier1`→"chicken
   coop"); loader rejection paths; theme-gate pass/fail/exit-code behavior;
   and the CORE/SKIN guard test (word-boundary grep of every engine module
   + theme_gate tool for egg-farm nouns, with a non-vacuousness self-check).
4. theme-gate v0 — `tools/theme_gate.py` (manifest shape via the loader +
   the seed card's string-budget idea: 256-char titles, 1024-char text,
   `#RRGGBB` embed colors; exit 1 on violation, 2 on empty themes/) and NEW
   workflow `.github/workflows/theme-gate.yml` on pull_request +
   push-to-main. Kit-owned `substrate-gate.yml` untouched.

Verify: `python3 -m pytest -q` → 24 passed; `python3 bootstrap.py check
--strict` green after this flip. Owner enabled repo auto-merge before this
session's PRs (OA-001): PR #1 armed auto-merge via GraphQL and merged on
substrate-gate green automatically.

## 💡 Session idea

`GeneratorSpec.base_rate` currently rides IN the theme pack, so a theme can
set any positive rate — the pre-registered-economy contract wants CORE to
own baseline numbers and themes to hold only bounded multipliers. Guard
recipe: move base rates into an engine-owned economy table
(`idle_engine/economy.py`), change `Theme.generator_specs()`
(idle_engine/theme.py) to apply a schema-bounded `rate_multiplier`, and add
a theme-gate rule + test (`tests/test_theme_gate.py`) rejecting multipliers
outside declared bounds. Belongs in ORDER 000 follow-up / economy slice.

## ⟲ Previous-session review

The seed card (2026-07-10-seed.md) parked one 💡: theme-gate should enforce
Discord string budgets, not just schema shape. Landed here — theme_gate v0
checks the 256/1024 caps and a test proves an oversized title fails the
gate. Seed friction on reachability (link everything at plant time) held:
no orphaned files added; new tooling is discoverable via tests + workflow.
One friction converted: the first pytest run red-flagged an assertion that
`tick(dt=0)` returns `{}` — the engine records a zero-valued balance entry
instead; contract clarified in the test (`sum(...) == 0`), engine left pure.
