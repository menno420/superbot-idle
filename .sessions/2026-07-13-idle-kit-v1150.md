# 2026-07-13 — kit upgrade v1.7.1 → v1.15.0: substrate-gate gains the `--added-card` born-red HOLD

> **Status:** complete

- **📊 Model:** fable-5 (Claude Fable family) · kit-upgrade distribution wave · session opened 2026-07-13T14:36Z (`date -u`)

## Scope

Upgrade the vendored substrate-kit from v1.7.1 to v1.15.0 so the kit-owned
`substrate-gate.yml` gains the `--added-card` born-red HOLD lane — the
v1.7.1 generation gates an advisory absent sentinel
(`.sessions/__born-red-card-added__.md`) and omits `--require-session-log`,
so an in-progress card ADDED by a PR never turns the required
`substrate-gate` context red (empirical: PR #89 pre-flip head `49753ed` →
gate run 29255821205 SUCCESS). The fix is live upstream: kit v1.15.0's gate,
running in superbot-games @ `d6a9526`. Routed here as the kit
distribution-wave upgrade that
`.sessions/2026-07-13-born-red-enabler-hardening.md` (PR #90) explicitly
deferred to.

Same branch, second scope item: the repo-owned `auto-merge-enabler.yml`
(HAND-INSTALLED, PR #77 `457407c`, hardened PR #90 `d29c4d9`) carries
warning text asserting main requires the `pytest` status context — false;
required contexts on main are `substrate-gate` + `theme-gate` only (enabler
run 29255821474 step "rules": `required contexts (2):
["substrate-gate","theme-gate"]`). Make the text truthful. If the kit
regeneration overwrites the enabler, re-apply the #77+#90 customizations
(the file's own header mandates exactly this).

Flow: the sanctioned two-command upgrade documented in superbot-games
`.sessions/2026-07-12-kit-upgrade-v1.15.0.md` — v1.15.0 dist
`bootstrap.py` (sha256
`25d22af9d9d81b2a7dc6d8d500234b6aa0f3f1c6a0400284ce2381baaeac650e`,
828,825 bytes, kit tag v1.15.0 = annotated `0b26d41` → `eaf4f23`) staged as
`bootstrap.py.new`, then `python3 bootstrap.py.new upgrade`, then
`python3 bootstrap.py upgrade --apply-docs`. Kit-owned files only — no
`control/inbox.md`, no `control/status.md` edits.

Branch `claude/idle-kit-v1150` · claim `control/claims/idle-kit-v1150.md`
· born-red card first commit, flip complete last.

## Verify

- `python3 bootstrap.py check --strict` with this card stashed
  (main-equivalent tree): exit 0 — `check: all checks passed.`
- With this card present, in-progress: exit 1 — `HOLD (by design)`;
  the CI added-card lane simulation (`--session-log
  .sessions/__born-red-card-added__.md --added-card <this card>`)
  also exit 1 with the `[session-card-hold] … born-red HOLD` finding —
  exactly the lane the v1.7.1 gate lacked. The upgrade delivers the
  designed red.
- `python3 -m pytest -q` → `1260 passed, 1 skipped in 17.00s` (exit 0).
- Regenerated `.github/workflows/substrate-gate.yml` byte-equal to
  superbot-games @ `d6a9526`; both workflows `yaml.safe_load` clean.
- Docs report count line (verbatim): `consumer-edited: 5 · diverged: 2 ·
  template-improved: 6 · unchanged: 10`; full report committed at
  `.substrate/upgrade-report.md`. Outgoing v1.7.1 dist banked at
  `.substrate/backup/bootstrap-1.7.1.py` (`6c24a8ae…`, byte-equal to
  `675c3471:bootstrap.py`).

## Close-out

Shipped as PR #91 — born-red commit `0f88069`, upgrade commit `9588747`,
enabler warning-text fix `e703fd3`. Kit v1.7.1 → v1.15.0; the required
`substrate-gate` context now holds an added in-progress card red
(GATE-RED-BY-DESIGN observed live on this PR's own pre-flip head
`e703fd3`: substrate-gate run 29259353167 FAILURE at 14:46:16Z while
theme-gate 29259353161 and pytest 29259353116 passed — the first
empirical fire of the added-card lane in this repo; contrast PR #89's
pre-flip SUCCESS run 29255821205 under v1.7.1). The kit
v1.15.0 regeneration claimed `auto-merge-enabler.yml` as kit-owned and
stripped the host card guard — REVERTED to the #77 (`457407c`) + #90
(`d29c4d9`) host version per the superbot-games precedent (games @
`d6a9526` also runs a host-installed enabler); kit's version banked
under `.substrate/`. Enabler warning text no longer asserts pytest is a
required context (it is not — run 29255821474: `required contexts (2):
["substrate-gate","theme-gate"]`; suite stays manual until ORDER 003's
⚑ lands). Lane-owed follow-ups: bump `control/status.md` `kit:` line to
v1.15.0 on the next heartbeat overwrite; consider splitting the host
enabler guard into a separate workflow so future kit upgrades stop
colliding with it. Claim `control/claims/idle-kit-v1150.md` released in
this flip commit.

## 💡 Session idea

Kit v1.15.0's carve-out scanner correctly DETECTED the host card-guard
step in `auto-merge-enabler.yml` ("host-added step … in job
'enable-auto-merge'") and even banked the pre-regen file — then
regenerated anyway, discarding the repo's only pre-upgrade born-red
hold, with the re-apply left to the operator. A carve-out hit on a file
whose host delta is a whole STEP (not a token tweak) should abort the
regeneration of that one file and report "kept host version — template
delta banked" instead: the kit already has both byte streams, and
keep-host-on-structural-carve-out is exactly what both live fleets
(games @ `d6a9526`, idle #77/#90) chose by hand. Guard recipe: kit-side,
the enabler branch of the carve-out scan in `bootstrap.py` upgrade path;
test target: an upgrade fixture with a host-added step asserting the
live file is kept byte-identical.

## ⟲ Previous-session review

Reviewed `.sessions/2026-07-13-born-red-enabler-hardening.md` (PR #90)
against live evidence: its layering call was exactly right — it refused
to hand-edit the kit-owned gate and routed the born-red HOLD as this
distribution-wave upgrade, which landed clean (gate byte-equal to games,
zero gate carve-outs). Its 💡 (enabler text falsely asserts pytest is
required, run 29255821474) was actionable as written and shipped here as
`e703fd3` — a model handoff: cited run id, exact false claim, exact
remedy. One gap it could not have foreseen: it assumed the enabler was
safe as a repo-owned file, but v1.15.0's regeneration CLAIMS the
filename as kit-owned — the re-apply duty its own header mandated fired
on the very next upgrade, this session.

