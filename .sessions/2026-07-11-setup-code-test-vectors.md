# 2026-07-11 — setup-code v1 test vectors (cross-language parity file)

> **Status:** `in-progress`

- **📊 Model:** fable-5 · high · idle-engine seat (test-vector builder, coordinator-assigned) · 2026-07-11T01:17Z– (`date -u`)

## What happened

(born-red — close-out pending)

Building the parked idea from slice (e)'s card: export a machine-readable
cross-language test-vector file so the websites lane can build a
byte-compatible setup-code encoder.

Planned deliverables:

1. `tests/vectors/setup-codes.v1.json` — versioned, language-agnostic:
   valid vectors (6 packs × bounded feature combos, with payload/crc16
   intermediates), decode-tolerance vectors, error vectors with taxonomy
   class names. Generated by `tools/gen_setup_vectors.py`.
2. `tests/test_setup_vectors.py` — regenerate-or-red (committed JSON must
   be byte-identical to an in-memory regeneration) + consumer replay of
   every vector through the real decoder.
3. `docs/provisioning.md` — short consumer-pointer section.

## 💡 Session idea

(pending close-out)

## ⟲ Previous-session review

(pending close-out — previous-session review to be written at close)
