# 2026-07-14 — preflight-script plant + host card-guard split out of the enabler

> **Status:** `complete`
> **Branch:** `claude/preflight-and-enabler-split` · claim PR #136 (merged 2026-07-14T18:05Z, squash `61f9856`)

- **📊 Model:** fable-5 · medium · feature build — plant `scripts/preflight.py` (local-ritual ↔ CI parity) + relocate the host card-guard/provenance customizations out of the kit-owned auto-merge-enabler into a separate host-owned workflow · session opened 2026-07-14T17:54Z (`date -u`)

**Goal:** two v1.16.0 follow-ups, both symptoms visible in `check --strict`
at HEAD `61f9856`:

1. **Plant `scripts/preflight.py`** — v1.16.0 seeds
   `preflight_scripts: ["scripts/preflight.py"]` into
   `substrate.config.json` (kit contract: `_default_preflight_scripts` +
   `_run_preflight_scripts`, `bootstrap.py`), and until the script exists
   every full-lane `check` run — local AND the CI substrate-gate — prints
   `NOTE — preflight script scripts/preflight.py not found — skipped`.
   The script runs this repo's real verify line (`python3 -m pytest -q` +
   `python3 tools/theme_gate.py themes`, the same invocations
   `pytest.yml`/`theme-gate.yml` run in CI) so the local ritual and the CI
   gate converge on one check list. Executes the previous session's 💡
   (`.sessions/2026-07-14-kit-upgrade-v1.16.0.md`).
2. **Make the host enabler customizations regen-proof** — the live
   `.github/workflows/auto-merge-enabler.yml` (sha256 `1a0c5ec3…ade18`) is
   kit-owned (regenerated in place on every adopt/upgrade) but carries three
   host customizations: the in-progress/drafted card-guard step, the
   4-prefix branch allowlist, and the `Head-ref:` squash-body provenance
   stamp. Every kit upgrade banks + clobbers them (v1.16.0 upgrade report
   carve-out section; restored by hand that session). Fix per the kit's own
   doctrine (template header + `_regen_kit_owned_workflow` report text:
   "move them into a separate workflow file"): branch allowlist →
   `substrate.config.json` `automerge.branch_patterns` (regen-preserved,
   quiets the `automerge-branch-drift` advisory); enabler file → regenerated
   byte-identical to the kit v1.16.0 template (future regens = no-op);
   card-guard + Head-ref stamp → new host-owned
   `.github/workflows/automerge-card-guard.yml` that reconciles arming state
   AFTER the enabler runs. Live behavior preserved: never armed (steady
   state) while an in-diff card is `in-progress`/`drafted`; only the four
   agent prefixes arm; squash auto-merge on non-draft PRs otherwise;
   squash bodies keep `Head-ref:`.

## What happened

Shipped via **PR #137** (`claude/preflight-and-enabler-split`; claim PR
#136 landed on main first per claim doctrine).

1. **`scripts/preflight.py` planted** — runs the repo's verify line
   byte-equal to CI (`python3 -m pytest -q` → pytest.yml;
   `python3 tools/theme_gate.py themes` → theme-gate.yml), self-installs
   the same deps those jobs pip-install (the substrate-gate venue installs
   none), exits non-zero on any failure with a clear last line (the
   runner's failure tail). The not-found NOTE no longer prints in
   `check --strict` — local ritual and CI gate now share ONE check list.
2. **Enabler split** — `substrate.config.json`
   `automerge.branch_patterns` now carries the live 4-prefix allowlist
   (`claim/*`, `claude/*`, `close-out/*`, `control/*`);
   `auto-merge-enabler.yml` regenerated byte-identical to the kit v1.16.0
   template output for those knobs (verified in-session:
   `automerge_enabler_workflow(*_automerge_params(config))` == live file
   — future regens are no-ops, the carve-out bank/restore cycle ends);
   the card-status guard + `Head-ref:` squash provenance relocated into
   host-owned `.github/workflows/automerge-card-guard.yml`, a reconciler
   that runs after the enabler's verdict on the same PR events (disarms
   while an in-diff card is `in-progress`/`drafted`; re-stamps `Head-ref:`
   when armed with final cards; never arms from scratch). The
   `automerge-branch-drift` advisory is quieted (config ↔ workflow terms
   now match). Old enabler sha256 `1a0c5ec3537eb3db43036a4127d86537a50ab012f756f052c5cf7469c21ade18`
   remains banked at `.substrate/backup/auto-merge-enabler.pre-regen-1a0c5ec3.yml`.
3. **Documented delta** (honest, not hidden): at PR open the kit enabler
   arms card-blind for up to ~1–2 min until the guard disarms; the merge
   stays blocked throughout by the red required substrate-gate (born-red
   hold), so no merge can slip the window. Steady-state behavior matches
   the old in-enabler guard.

## Verify at flip

- `python3 -m pytest -q` → `1381 passed, 1 skipped` (baseline-identical
  to clean HEAD `61f9856`)
- `python3 tools/theme_gate.py themes` → `theme-gate: all 18 pack(s)
  valid (schema v1)`
- `python3 scripts/preflight.py` → exit 0, `preflight: all checks passed
  (pytest + theme-gate)`
- `python3 bootstrap.py check --strict` → preflight NOTE gone,
  `automerge-branch-drift` advisory gone; pre-flip exit 1 ONLY on this
  card's designed born-red hold (pre-existing `model-line-class`
  advisories on six older cards are untouched scope)

## Lane-owed

- `control/status.md` heartbeat `kit:` line still reads v1.15.0 (tree is
  v1.16.0) — the bump stays owed to the next status overwrite session (no
  inbox ORDER at HEAD covers a re-stamp this session; ORDER 008 was served
  and closed by PR #132).

## 💡 Session idea

Two hosts (this repo and superbot-games' hardened guard @ `d6a9526`) now
hand-carry the SAME card-status guard the kit's generated enabler lacks —
and the born-red card convention the guard protects is itself kit doctrine
(the substrate-gate added-card lane). Promote the guard into the kit:
a config knob `automerge.card_guard: true` making
`automerge_enabler_workflow()` emit the card-skip step (and, while there,
`automerge.squash_body: "Head-ref: {head_ref}"` for the provenance stamp),
so hosts stop needing reconciler workflows for kit-doctrine behavior.
Guard recipe: kit `automerge_enabler_workflow` + `_automerge_params`
(vendored `bootstrap.py`, the arm step's `gh pr merge` line); config keys
beside `branch_patterns`; test target: template renders the card step /
`--body` only when configured, and `check_automerge_preflight` stays
branch-terms-only so the new knobs don't re-open the drift nag.

## ⟲ Previous-session review

previous-session review: `.sessions/2026-07-14-kit-upgrade-v1.16.0.md`
(PR #134) — its carve-out prediction was exact (the regen banked the
enabler at `pre-regen-1a0c5ec3.yml`, precisely as the v1.15.0 session had
predicted), and its restore trail plus its 💡 (plant the preflight) handed
this session both tasks fully scoped — zero re-derivation needed. The
honest gap: it restored the carve-out by hand instead of ending the cycle,
deferring the split debt to this session; defensible on a distribution-wave
day (kit-owned files only was its stated scope), and it said so explicitly
rather than silently.
