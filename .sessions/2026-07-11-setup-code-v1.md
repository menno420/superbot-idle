# 2026-07-11 — slice (e): SETUP-CODE FORMAT v1 (provisioning contract)

> **Status:** `complete`

- **📊 Model:** fable-5 · high · idle-engine seat (slice (e) builder, coordinator-assigned) · 2026-07-11T00:49Z–00:5xZ (`date -u`)

## What happened

Shipped the versioned provisioning contract the websites lane consumes, in
one build PR after a control fast-lane claim (PR #15,
`control/claims/setup-code-v1.md`, merged then removed here).

1. **`idle_engine/provisioning.py`** — pure, deterministic, stdlib-only.
   `encode_setup(SetupConfig) -> str` / `decode_setup(str) -> SetupConfig`:
   code = `IDLE1-` + Crockford base32 of `flags(1B) ++ theme_id(ASCII slug)
   ++ crc32&0xFFFF(2B, BE)`. Flags are HONEST — one bit per mechanic the
   engine actually ships (offline_progress / upgrades / prestige), no
   reserved vaporware bits. Decode is paste-tolerant (case-insensitive,
   internal hyphens ignored, `O`→`0` / `I`,`L`→`1` folded) but canonical on
   encode (same config → same string, byte for byte). Catalog resolution is
   a deliberately SEPARATE step, `validate_against_catalog(config,
   themes_dir)`, so the plugin can decode at its trust boundary with zero
   I/O; it also rejects a toggled-on feature the resolved pack declares no
   skin for (upgrades/prestige blocks are optional in schema v1).
   Distinct error taxonomy, every class tested: Malformed / UnknownVersion /
   Checksum / UnknownFeature / UnknownTheme / FeatureUnavailable /
   InvalidConfig, all under `SetupCodeError(ValueError)`.
2. **`docs/provisioning.md`** — the consumption contract: byte-by-byte
   grammar, field table, v1-FROZEN versioning policy (additive = `IDLE2-`;
   v1 decoders fail loud on future versions/bits, never misparse), worked
   bytes walkthrough, literal encoder-generated example codes for all three
   shipped packs, error taxonomy, and the consumers seam named explicitly
   (websites lane = encoder, superbot-next plugin = decoder). Cross-linked
   from `docs/AGENT_ORIENTATION.md` at birth — slice (d)'s orphan-doc
   lesson applied proactively, no red loop.
3. **Tests 96 → 170** (`tests/test_provisioning.py`, 74 tests): round-trip
   3 shipped themes × all 8 flag combos, determinism, paste tolerance,
   payload AND checksum-region tamper → ChecksumError, non-canonical
   padding, unknown version/feature-bits/theme, feature-unavailable via a
   minimal no-upgrades/no-prestige pack, encoder slug rejection, and
   regenerate-or-red doc honesty: every literal code in the doc's examples
   table must decode to its stated config AND be exactly what the live
   encoder emits (md-parity pattern from `test_theme_schema.py`); the
   worked-bytes walkthrough values are pinned against `zlib.crc32` too.
4. Engine core stays noun-free — `provisioning.py` is scanned by
   `test_core_skin_guard.py` automatically (glob `idle_engine/**/*.py`) and
   passes; theme vocabulary appears only in docs/tests.

Verify: `python3 -m pytest -q` → 170 passed; `python3 bootstrap.py check
--strict` green; `python3 tools/theme_gate.py themes` → all 3 packs PASS.

## 💡 Session idea

The doc's examples table is de-facto the cross-language test-vector set:
the websites lane will reimplement the ENCODER in its own stack, and prose
+ five literals under-specify edge behavior (padding-bit canonicality,
look-alike folding, slug bounds). Guard recipe: when superbot-web starts
its encoder, export a machine-readable vector file (e.g.
`tests/vectors/setup-codes-v1.json`: config → code, plus invalid-code →
error-class rows) generated BY `tests/test_provisioning.py` helpers, and
have both repos' suites consume the same committed file — contract drift
then reds both sides instead of surfacing as a live decode failure.

## ⟲ Previous-session review

Slice (d)'s card (2026-07-11-economy-design-v1.md) parked committing
`tools/pacing_sim.py` until SIM-001 results return — still correctly
parked (SIM-001 ⚑ is in flight, PR #13). Its implicit lesson (a new doc
must be reachable from the read path or strict reds) was applied here at
birth rather than as a fix-up commit. Slice (c)'s roster-divergence flag
(packs skin the same opaque ids by convention only) touches this slice:
`validate_against_catalog` deliberately checks per-pack skin PRESENCE
(upgrades/prestige blocks), not roster identity — roster unification stays
with the economy schema-revision as (c) scheduled it, and setup codes stay
mechanics-agnostic (they name a pack + toggles, never numbers).
