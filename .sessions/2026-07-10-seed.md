# 2026-07-10 — seed session: superbot-idle born-right

> **Status:** `complete`

- **📊 Model:** fable-5 · high · repo-seed (dispatch copilot, superbot live dispatch chat) · 2026-07-10 ~23:3xZ (`date -u`)

## What happened

Owner created the empty repo (~22:21Z); the dispatch copilot seeded it on the fleet
recipe (fourth consumer): kit v1.7.1 `dist/bootstrap.py` → `adopt` → all 9 interview
slots answered with REAL design values (core/skin split, theme-gate, pre-registered
economy numbers, plugin-native, Q-0266 volume-first owner profile) → `render --live`
(0 unfilled) → `mode guided` → `substrate-gate.yml` wired into `.github/workflows/` →
lane layer planted (README contract · CONVENTIONS · PLATFORM-LIMITS · themes/README ·
review-queue · docs/retro/questions, all linked from docs/AGENT_ORIENTATION.md — the
sim-lab reachability lesson applied at seed, not after) → real status heartbeat with
OA-001 (settings six-field) → this card → `check --strict` green → the one allowed
push to empty main.

## 💡 Session idea

The theme-gate should validate not just schema-shape but STRING-BUDGETS (Discord embed
field caps: 256-char titles, 1024-char field values, 25 fields) — a theme that passes
the gate must not overflow an embed at render time; encode the caps in the schema so
the gate catches them mechanically. Belongs in ORDER 000 → slice (a).

## ⟲ Previous-session review

The sim-lab seed (same recipe, part 4) hit the reachability wall AFTER planting
(docs/retro/questions.md orphaned) and fixed it reactively; this seed applied the
lesson proactively — every docs-tree file linked from AGENT_ORIENTATION at plant time,
and `check --strict` ran as the direct `&&` predecessor of the push per the recipe's
part-3 lesson. No new friction to convert; the recipe held on its fourth run.
